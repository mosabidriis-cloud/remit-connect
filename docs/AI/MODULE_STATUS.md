# Module Status

Status of every REOS module. Grounded in git tags and the presence of components/services/types/pages under `src/features/reos` observed during documentation review. This file does not replace CURRENT_SPRINT.md as the authority on active sprint scope.

| Module | Status | Current Sprint | Completion % | Notes |
|---|---|---|---|---|
| Enterprise UI | COMPLETE | Sprint 10 | 100% | Application shell, layout, and design tokens. Tags v0.10.0-shell through v0.10.6-dev-bootstrap. |
| Shared Batch Upload | COMPLETE, FEEDS BRANCH PROCESSING | Sprint 11 | 100% | Upload, validation, and shared batch creation implemented (`sharedBatchService.ts`, `excelValidationService.ts`, `SharedBatchUploadPage.tsx`). Tags v0.11.0 through v0.11.3-shared-batch-complete. Assignment step now delegates to the canonical `branchAssignmentService.assignSharedBatchToBranch` (Sprint 14 Milestone 1.5, DECISIONS.md DEC-006) instead of creating Assignments inline - this remains the only pathway with real uploaded beneficiary data, so it is the only one that produces Assignments with real transactions. |
| Branch Assignment | COMPLETE, USES CANONICAL ASSIGNMENT SERVICE | Sprint 12 | 100% | Assignment engine and UI implemented (`branchAssignmentService.ts`, `assignmentService.ts`, `BranchAssignmentPage.tsx`). Tags v0.12.0 through v0.12.3-branch-assignment-complete. `branchAssignmentService.ts` is now the canonical Assignment workflow (Sprint 14 Milestone 1.5) - it enforces role/lock rules and also builds the `Assignment` record. Limitation: `BranchAssignmentPage.tsx` only ever operates on a self-constructed `SharedBatch` with no real beneficiary data, so its Assignments have zero real transactions and won't populate Branch Processing's queue. See TECH_DEBT.md. |
| Branch Processing | STABILIZED, WIRED INTO WORKFLOW | Sprint 13 (closed for sequencing) | ~95% (estimate) | Queue, engine, and completion implemented (`transactionProcessingService.ts`, `branchProcessingQueueService.ts`, `BranchProcessingPage.tsx`). Stabilized in Sprint 13 (proof/reason gating, shared theme tokens, `COMPLETED` terminology). Sprint 14 Milestone 1 connected it to the rest of the workflow via `sharedBatchStore.ts`; Milestone 1.5 verified `BranchProcessingPage.tsx` needed no change - it already consumed `Assignment` objects exclusively. Open decisions carried forward: (1) no actor-role gating exists in Branch Processing, unlike `branchAssignmentService.ts`/`proofDownloadService.ts`; (2) whether to unify the two structurally-similar completion/return validation implementations. See CURRENT_SPRINT.md and TECH_DEBT.md. |
| Proof Management | WIRED INTO WORKFLOW, COMPLETED -> READY_FOR_DOWNLOAD STILL MANUAL/UNIMPLEMENTED | Sprint 14 (current - Milestone 1 complete) | ~80% (estimate) | Substantial legacy implementation predates Sprint 10-13 numbering (tag `v0.7-proof-download`): upload (`ProofUpload.tsx`, `proofOfPaymentService.ts`) and a functionally complete download side (`proofDownloadService.ts`, already role-gated to Direct Remit Officer; `ProofDownloadPage.tsx`). Design recorded in PROOF_MANAGEMENT.md (Milestone 0). Sprint 14 Milestone 1 added `buildProofDownloadBatchFromSharedBatch` (`proofDownloadService.ts`) so `ProofDownloadPage.tsx` resolves a real batch from its route's `:batchId` param via `sharedBatchStore.ts`, and writes `DOWNLOADED` back to the store on confirm. Remaining gap: nothing implements `COMPLETED -> READY_FOR_DOWNLOAD`, so a real batch reaching `COMPLETED` via Branch Processing is correctly blocked from download by existing validation until that transition is built (Open Decision 2, CURRENT_SPRINT.md) - this is expected, not a bug. 4 components (`ProofDownloadPanel`, `BatchDownloadSummary`, `DownloadHistory`, `BatchDownloadActions`) still on raw Tailwind. |
| Reporting | PARTIAL, PRE-DATES CURRENT ROADMAP | Not in Sprint 10-14 numbering | Not quantified | Volume Reports are implemented (`reportService.ts` volume definitions, `ReportsPage.tsx` wired with filters/summary/table). Performance Reports category is defined in `report.ts` types (`BRANCH_PERFORMANCE`, `OFFICER_PERFORMANCE`, `PROOF_COMPLETION`, etc.) but has no report definitions in `reportService.ts`. Export actions (Excel/PDF/Print) render but are disabled in `ReportExportActions.tsx`. Built under legacy tags `v0.9.1-reporting-foundation` / `v0.9.2-volume-reports`; not reflected in ROADMAP.md's Sprint 10-14 Completed list. Downstream consumer of Proof Management (PROOF_MANAGEMENT.md, Integration Points). See TECH_DEBT.md. |
| Administration | NOT STARTED | Not yet scheduled | 0% | No branch/settings/master-data administration components or pages were found under `src/features/reos` during this review. |
| Production Readiness | NOT STARTED | Not yet scheduled | 0% | No dedicated implementation found. |

## Open Item

Proof Management and Reporting have real, substantial implementations in the repository (predating the Sprint 10 Enterprise UI reset) that are not fully represented in ROADMAP.md's sprint-numbering sequence (Proof Management is now formally Sprint 14; Reporting remains unscheduled). This is a documentation/sequencing gap, not a code gap. Recommend a business decision on how to fold Reporting into the current sprint-numbering scheme.
