# Module Status

Status of every REOS module. Grounded in git tags and the presence of components/services/types/pages under `src/features/reos` observed during documentation review. This file does not replace CURRENT_SPRINT.md as the authority on active sprint scope.

| Module | Status | Current Sprint | Completion % | Notes |
|---|---|---|---|---|
| Enterprise UI | COMPLETE | Sprint 10 | 100% | Application shell, layout, and design tokens. Tags v0.10.0-shell through v0.10.6-dev-bootstrap. |
| Shared Batch Upload | COMPLETE, FEEDS BRANCH PROCESSING, VERIFIED LIVE | Sprint 11 | 100% | Upload, validation, and shared batch creation implemented (`sharedBatchService.ts`, `excelValidationService.ts`, `SharedBatchUploadPage.tsx`). Tags v0.11.0 through v0.11.3-shared-batch-complete. Assignment step delegates to the canonical `branchAssignmentService.assignSharedBatchToBranch` (Sprint 14 Milestone 1.5, DECISIONS.md DEC-006) - the only pathway with real uploaded beneficiary data, so the only one that produces Assignments with real transactions. Sprint 15 M1.5 added `BranchProcessingNavigation.tsx`, a real link from a finalized Assignment to that branch's processing queue. Sprint 15 M1 verified the upload-through-assignment flow live in a browser for the first time. Sprint 16 M4.1 added two read-only enumerators to `sharedBatchStore.ts` (`getAllSharedBatches`, `getAllAssignments`, DEC-007) so Reporting can read enterprise-wide; no existing behavior changed and this module's workflow is unaffected. |
| Branch Assignment | COMPLETE, USES CANONICAL ASSIGNMENT SERVICE | Sprint 12 | 100% | Assignment engine and UI implemented (`branchAssignmentService.ts`, `assignmentService.ts`, `BranchAssignmentPage.tsx`). Tags v0.12.0 through v0.12.3-branch-assignment-complete. `branchAssignmentService.ts` is now the canonical Assignment workflow (Sprint 14 Milestone 1.5) - it enforces role/lock rules and also builds the `Assignment` record. Limitation: `BranchAssignmentPage.tsx` only ever operates on a self-constructed `SharedBatch` with no real beneficiary data, so its Assignments have zero real transactions and won't populate Branch Processing's queue. See TECH_DEBT.md. |
| Branch Processing | STABILIZED, WIRED INTO WORKFLOW, VERIFIED LIVE | Sprint 13 (closed for sequencing) | ~97% (estimate) | Queue, engine, and completion implemented (`transactionProcessingService.ts`, `branchProcessingQueueService.ts`, `BranchProcessingPage.tsx`). Stabilized in Sprint 13 (proof/reason gating, shared theme tokens, `COMPLETED` terminology). Sprint 14 connected it to the rest of the workflow via `sharedBatchStore.ts`. Sprint 15 found and fixed a critical defect via live testing: `hydrateBranchProcessingQueue` was not idempotent and silently discarded completed work (status + proofs) on every remount - fixed to preserve existing progress; verified live against the exact reproduction that found it (M1.75). Sprint 15 M1.5 also added the missing navigation link from Assignment to this module. Sprint 16 M4.1 added one read-only enumerator, `getAllBranchProcessingQueueItems` (DEC-007), returning deep copies and deliberately never calling `hydrateBranchProcessingQueue`; the existing per-branch `getBranchProcessingQueue` was left returning live references, because this module mutates queue items in place and copying there would break processing. Open decisions carried forward: (1) no actor-role gating exists in Branch Processing, unlike `branchAssignmentService.ts`/`proofDownloadService.ts`; (2) whether to unify the two structurally-similar completion/return validation implementations; (3) no processing timestamps or actor attribution are recorded, blocking every duration-based reporting metric (REPORTING_ARCHITECTURE.md Decision D-6). See TECH_DEBT.md. |
| Proof Management | COMPLETE - SPRINT 14 + 15 CLOSED, VERIFIED LIVE END TO END | Sprint 15 (closed) | ~98% (estimate) | Substantial legacy implementation predates Sprint 10-13 numbering (tag `v0.7-proof-download`): upload (`ProofUpload.tsx`, `proofOfPaymentService.ts`) and download (`proofDownloadService.ts`, role-gated to Direct Remit Officer; `ProofDownloadPage.tsx`). Design in PROOF_MANAGEMENT.md. Sprint 14 implemented the full `ASSIGNED -> PROCESSING -> COMPLETED -> READY_FOR_DOWNLOAD -> DOWNLOADED` chain in code, added the Branch Processing -> Proof Download navigation, and removed the circular dependency between the two. Sprint 15 M1 through M1.75 **proved this chain works at runtime for the first time** (Sprint 14's own verification was entirely static) - uploaded a real batch, assigned it, processed both transactions with real proof images, finalized, and confirmed Proof Download showed the correct 2 completed transactions and 2 proof images, surviving realistic browser navigation. Sprint 15 M2 migrated the last 4 Tailwind components (`ProofDownloadPanel`, `BatchDownloadSummary`, `DownloadHistory`, `BatchDownloadActions`) to shared theme tokens, verified live/screenshot-inspected. Sprint 15 M4 fixed `downloadStatus` mislabeling a `COMPLETED` batch. Remaining, out of scope: a placeholder nav link in `layout/Sidebar.tsx` (different module); `proofOfPaymentService.markProofDownloaded` still orphaned (never called); a minor reactivity gap where the Branch Processing -> Proof Download link doesn't appear until the page is revisited after Finalize (data is correct once revisited). See TECH_DEBT.md. |
| Reporting | WORKING END TO END, VERIFIED LIVE | Sprint 16 | ~85% (estimate) | Originally built under legacy tags `v0.9.1-reporting-foundation` / `v0.9.2-volume-reports` with six Volume definitions and a working engine. **Sprint 16 M1 found the module had no data source at all**: `ReportsPage.tsx` read from React Router `location.state`, nothing navigated there with state, so every report rendered permanently empty for a real user. Sprint 16 then designed (REPORTING_ARCHITECTURE.md, REPORTING_PROJECTION_LAYER.md) and built the fix across four milestones. **M4.1**: read-only enumerators on `sharedBatchStore` and `branchProcessingQueueService` (DEC-007). **M4.2**: `reportingProjectionService.ts` + `types/reportingProjection.ts` - four async read-only operations producing `BatchReportProjection`, `BranchReportProjection`, `ProcessingReportProjection`, `ProofReportProjection`, reusing every existing summary rather than recomputing it, returning frozen output, enforcing Branch Officer scope in the layer. **M4.3**: Report Service - four async generate operations consuming only the projection layer, one consolidated `matchesFilters` shared with the legacy path, and the first two `PERFORMANCE` definitions (`BRANCH_PERFORMANCE`, `PROOF_COMPLETION`). **M4.4**: `ReportsPage.tsx` migrated off `location.state` onto `reportService`, with loading/empty/error handling built from existing components (none modified) and all legacy dead code removed. **Verified live in a browser across Upload -> Assignment -> Processing -> Proof -> Reports**: all 8 definitions return correct real data, filters work, the empty state works, reports track lifecycle transitions live, and no earlier workflow regressed; a duplicate-React-key defect invisible to `tsc`/`build` was found and fixed. **The M1 headline defect is closed for Reporting.** Still open: exports are disabled placeholders (D-3), no Audit category (D-5), no duration or officer metrics (D-6), branch identity derives from assignments only (D-7), category taxonomy unresolved (D-1). Downstream consumer of Proof Management (PROOF_MANAGEMENT.md, Integration Points). See TECH_DEBT.md. |
| Dashboards | IMPLEMENTED BUT NOT CONNECTED; CONTAINS OUT-OF-SCOPE METRICS | Sprint 16 (design only) | Not quantified | Not previously listed as a module. Substantial implementation exists and predates the current sprint numbering: `dashboardService.ts`, `types/dashboard.ts`, `OperationsDashboardPage.tsx` (route `/reos/dashboard`, mounted), plus `BranchPerformanceTable`, `WorkQueueTable`, `ExceptionCenter`, `TodaySummary`, `CriticalAlertCard`. **Still carries the M1 defect Reporting has now shed**: `OperationsDashboardPage.tsx` sources data from `location.state`, so every KPI renders `0` when reached from the Sidebar. M4.4 proved the fix shape for this - route it through a service that reads the projection layer - but dashboards were out of scope and remain unapproved (D-2). Additionally displays Revenue and USD Value/USD Processed, which REPORTING_STANDARDS.md places out of scope, and references `GENERAL_MANAGER`, which is not an approved REOS role. Sprint 16 designed Executive and Branch dashboards and an extension path for the existing Operations Dashboard. See REPORTING_ARCHITECTURE.md Section 10 and TECH_DEBT.md. |
| Administration | NOT STARTED | Not yet scheduled | 0% | No branch/settings/master-data administration components or pages were found under `src/features/reos` during this review. |
| Production Readiness | NOT STARTED | Not yet scheduled | 0% | No dedicated implementation found. |

## RESOLVED - REOS page mounting (Sprint 14)

A critical defect found during Sprint 14 Milestone 2B: the application's real router `src/routes/AppRoutes.tsx` never mounted `BranchProcessingPage`, `SharedBatchUploadPage`, `BranchAssignmentPage`, `TransactionProcessingPage`, or `ReportsPage`, while `src/features/reos/routes/ReosRoutes.tsx` defined them as dead code nothing imported. Those pages therefore did not ship, despite compiling and building cleanly.

Resolved by the Sprint 14 Route Integration Audit (five routes mounted in the live router) and Stabilization & Closure (`ReosRoutes.tsx` deleted). Verified against the production bundle at the time: previously-absent page strings were present.

Sprint 15 M1 went further and verified this by actually using the app in a browser (upload, assign, navigate) rather than inspecting the bundle - confirming the fix holds for a real user journey, not just that the code is present. Sprint 15 also found (M1) and fixed (M1.5, M1.75) two further defects that bundle-content and `tsc`/`build` checks alone could not have caught: a missing navigation link, and a data-corrupting hydration bug.

Lesson recorded in PROJECT_STATE.md as a standing caveat - a passing build does not prove changed code ships, is reachable, or preserves data correctly.

## Open Item

Proof Management and Reporting have real, substantial implementations in the repository (predating the Sprint 10 Enterprise UI reset) that are not fully represented in ROADMAP.md's sprint-numbering sequence. Proof Management is now formally Sprint 14; Reporting is now formally Sprint 16 (architecture), which partially closes this gap. The Dashboards implementation remains unscheduled and is newly recorded above as its own module row - Sprint 16 found it in the same state as Reporting (implemented, unconnected), and it additionally carries metrics and a role that are out of scope. Recommend a business decision on whether Dashboards is a distinct module or part of Reporting.
