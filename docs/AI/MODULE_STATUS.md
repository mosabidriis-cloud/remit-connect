# Module Status

Status of every REOS module. Grounded in git tags and the presence of components/services/types/pages under `src/features/reos` observed during documentation review. This file does not replace CURRENT_SPRINT.md as the authority on active sprint scope.

| Module | Status | Current Sprint | Completion % | Notes |
|---|---|---|---|---|
| Enterprise UI | COMPLETE | Sprint 10 | 100% | Application shell, layout, and design tokens. Tags v0.10.0-shell through v0.10.6-dev-bootstrap. |
| Shared Batch Upload | COMPLETE, FEEDS BRANCH PROCESSING, VERIFIED LIVE INCLUDING REAL-FILE IMPORT | Sprint 11, Sprint 17 closed | 100% | Upload, validation, and shared batch creation implemented (`sharedBatchService.ts`, `excelValidationService.ts`, `SharedBatchUploadPage.tsx`). Tags v0.11.0 through v0.11.3-shared-batch-complete. Assignment step delegates to the canonical `branchAssignmentService.assignSharedBatchToBranch` (Sprint 14 Milestone 1.5, DECISIONS.md DEC-006). Sprint 15 M1.5 added `BranchProcessingNavigation.tsx`; Sprint 15 M1 verified upload-through-assignment live. Sprint 16 M4.1 added two read-only enumerators to `sharedBatchStore.ts` (DEC-007), no behavior change. **Sprint 17 reopened this module after a real production Direct Remit export was analysed and run through `excelValidationService`: it imported 0 of 63 real transactions** (all six required columns reported missing, header on the wrong row, amounts unparseable, bank/account split across two different layouts, no date captured). Five business decisions were approved (DEC-008 to DEC-012), implemented across three completed milestones. **M1 (Row Structure Detection):** the header is located rather than assumed, and leading blank rows, repeated page headers and blank data rows are skipped, with issues citing true spreadsheet row numbers. **M2 (Column Contract, DEC-013):** alias-based column resolution so the legacy and Direct Remit contracts share one import path; structural transaction-row detection (a row is a transaction only if it carries a reference, which removes the `TOTAL` subtotal rows without matching display text); tolerant amount parsing. **M3 (Bank Field, DEC-012, DEC-010):** `excelValidationService.ts` now calls the single shared `sharedBatchService.parseBankField` - previously used only by the unused CSV path - instead of requiring separate `Bank Name`/`Account Number` columns; the parser was extended to recognise the export's `"BANK OF KHARTOUM (Acc No: 4734114)"` form and its bare-account-number-plus-`Dest Country`-fallback form, with bank names preserved exactly as written (verified against nine real spellings of one bank across two scripts). **Real-file progression across M1-M3: records 69 -> 66 -> 63 (exactly the real transaction count); missing columns 6 -> 2 -> 0; valid records 0 -> 0 -> 63/63; `readyForAssignment` false -> true.** A legacy-format file validates 2/2 valid unchanged at every milestone. **M4 (Transaction Date, DEC-011): COMPLETE (2026-08-05).** The export's `Date` column (`DD/MM/YYYY`) is now captured into `Beneficiary.transactionDate`, converted to ISO so it sorts and parses correctly against the existing `reportService`/`dashboardService` logic that already assumed ISO. Not required for row validity - an absent or unparseable date leaves `transactionDate: ""` unchanged. Verified with a real in-memory `.xlsx` buffer through the actual service. **M5 (Stabilization & Closure): COMPLETE (2026-08-06).** The full workflow (Upload -> Assign -> Branch Processing -> Finalize -> Reports -> Dashboard) was driven live in a real headless browser via real client-side navigation against a synthetic file matching the real Direct Remit contract - 11/11 assertions passed, zero console errors, no application defect found. `npx eslint` re-run project-wide for the first time since Sprint 15; all findings pre-existing (TECH_DEBT.md). **Sprint 17 is fully complete, M1 through M5, and the real file imports successfully end to end including transaction dates.** **2026-08-09 (DEC-019, Production Readiness Phase 2 Milestone 1):** the Shared Batch and Beneficiary data this module creates is now Supabase-backed (`shared_batches`, `beneficiaries`), not in-memory - `saveSharedBatch`/`saveBeneficiaries` are now `async`, same signatures otherwise. See MODULE_STATUS.md's Branch Assignment row and OPERATIONAL_PERSISTENCE.md. |
| Branch Assignment | COMPLETE, USES CANONICAL ASSIGNMENT SERVICE; AUTHORIZATION HOTFIXED AND WORKFLOW COMPLETED 2026-08-03 | Sprint 12, hotfixed and completed out-of-sprint | 100% | Assignment engine and UI implemented (`branchAssignmentService.ts`, `assignmentService.ts`, `BranchAssignmentPage.tsx`). Tags v0.12.0 through v0.12.3-branch-assignment-complete. `branchAssignmentService.ts` is now the canonical Assignment workflow (Sprint 14 Milestone 1.5) - it enforces role/lock rules and also builds the `Assignment` record. **2026-08-03 hotfix:** `assignSharedBatchToBranch`'s role check and its one live call site (`SharedBatchUploadPage.tsx`) were changed from Direct Remit Officer to **Operations Manager**, on explicit instruction, after a runtime verification found a mismatch against the actor-role assignment specified for the fix. `reassignSharedBatch` (Operations Manager) is unchanged. **This reverses DEC-006 and BUSINESS_RULES.md as currently written; neither document has been updated to match** - see CURRENT_SPRINT.md, "Out-of-Sprint Hotfix" for detail. **2026-08-03 workflow completion:** the previously-recorded limitation below is now resolved - `sharedBatchStore.ts` gained `saveBeneficiaries`/`getBeneficiaries` (same pattern as its existing `SharedBatch`/`Assignment` accessors), `SharedBatchUploadPage.tsx` now persists real beneficiaries at upload time, and `BranchAssignmentPage.tsx` loads real `UNASSIGNED` batches via `getAllSharedBatches()`, displays them for selection, and assigns using their real beneficiaries via `getBeneficiaries()` - the hardcoded `beneficiaries: []` and the fabricated-batch fallback are both gone. `BranchAssignmentForm.tsx` was trimmed to branch selection (+ reassignment reason); manual ID entry is gone. Verified by a 10-assertion functional smoke test against the real, unmodified `branchAssignmentService.ts`/`branchProcessingQueueService.ts` - see CURRENT_SPRINT.md. Previous limitation, now resolved: ~~`BranchAssignmentPage.tsx` only ever operates on a self-constructed `SharedBatch` with no real beneficiary data, so its Assignments have zero real transactions and won't populate Branch Processing's queue.~~ **2026-08-09 (DEC-019, Production Readiness Phase 2 Milestone 1):** Assignment data is now Supabase-backed (`assignments` table) - `saveAssignment`/`getAssignment`/`getAssignmentsByBranch`/`getAllAssignments` are `async`, same signatures. `assignedTransactions`/`manualReviewTransactions`/`invalidTransactions` are deliberately not stored a second time - reconstructed at read time from `beneficiaries`, since `processingStatusId` never mutates after creation. Verified live across two separate browser contexts - a brand-new session showed the real assigned transaction, the definitive persistence proof. See TECH_DEBT.md and OPERATIONAL_PERSISTENCE.md. |
| Branch Processing | STABILIZED, WIRED INTO WORKFLOW, VERIFIED LIVE | Sprint 13 (closed for sequencing) | ~97% (estimate) | Queue, engine, and completion implemented (`transactionProcessingService.ts`, `branchProcessingQueueService.ts`, `BranchProcessingPage.tsx`). Stabilized in Sprint 13 (proof/reason gating, shared theme tokens, `COMPLETED` terminology). Sprint 14 connected it to the rest of the workflow via `sharedBatchStore.ts`. Sprint 15 found and fixed a critical defect via live testing: `hydrateBranchProcessingQueue` was not idempotent and silently discarded completed work (status + proofs) on every remount - fixed to preserve existing progress; verified live against the exact reproduction that found it (M1.75). Sprint 15 M1.5 also added the missing navigation link from Assignment to this module. Sprint 16 M4.1 added one read-only enumerator, `getAllBranchProcessingQueueItems` (DEC-007), returning deep copies and deliberately never calling `hydrateBranchProcessingQueue`; the existing per-branch `getBranchProcessingQueue` was left returning live references, because this module mutates queue items in place and copying there would break processing. **2026-08-05 (Decision D-6, resolved):** `BranchProcessingQueueItem` now records `startedAt`/`completedAt`/`completedByUserId`/`returnedAt`/`returnedByUserId`, set by `completeBranchProcessingQueueItem`/`returnBranchProcessingQueueItem`/the `IN_PROGRESS` transition. Purely additive in-memory state - no persistence. Verified by an 11-assertion functional smoke test through the real chain, including the downstream dashboard fix. See CURRENT_SPRINT.md. Open decisions carried forward: (1) **PARTIALLY RESOLVED (2026-08-08, DEC-018):** the route to Branch Processing is now real RBAC (`BranchGate` - Branch Officer confined to their own branch, Operations Manager any branch), and the page passes the real signed-in session's `userId` - but `branchProcessingQueueService.ts`'s write functions still perform no role check of their own, since the store is still in-memory with no RLS to fall back on (Production Readiness Phase 2); (2) whether to unify the two structurally-similar completion/return validation implementations. **2026-08-09 (DEC-019):** `hydrateBranchProcessingQueue`/`finalizeBranchProcessing` are now `async` (they read/write the now-Supabase-backed Shared Batch/Assignment data), but this module's own queue-item state (`branchProcessingQueueState`) is deliberately unchanged and still in-memory - a later, separately-scoped Phase 2 milestone. `BranchProcessingQueue.tsx`'s hydration call moved from a bare `useMemo` (a pre-existing lint finding) to a proper `useEffect`, closing that debt item as a forced side effect (a `useMemo` callback cannot be `async`). **2026-08-09 (DEC-020, Production Readiness Phase 2 Milestone 2): this module's queue-item state and branch-level lock are now Supabase-backed** (`branch_processing_queue_items`, `branch_processing_status`) - open decision (1) above is now fully resolved, since real RLS now backs every write, not just the route gate. `addProofToBranchProcessingQueueItem`'s signature changed to take a raw `File` (real Storage upload requires the bytes). The confirmed-dead `TransactionProcessingPage.tsx` (a second, un-migrated implementation of this same workflow, reachable by no in-app link) and its exclusively-used dependents were deleted rather than migrated. Verified live: a real proof image uploaded during processing loaded correctly via a Supabase Storage signed URL, and a brand-new Branch Officer session (zero uploads in it) showed the same transaction COMPLETED. **2026-08-13 (DEC-022, Production Readiness Phase 3): every write here (start/hold/complete/return/finalize/proof-upload) now records a real audit event** via `auditService.recordAuditEvent` - closes open decision (2)'s underlying visibility gap (every transition is now attributable and durable, not just the two structurally-similar validation implementations). See TECH_DEBT.md, OPERATIONAL_PERSISTENCE.md, and AUDIT_TRAIL.md. |
| Proof Management | COMPLETE - SPRINT 14 + 15 CLOSED, VERIFIED LIVE END TO END | Sprint 15 (closed) | ~98% (estimate) | Substantial legacy implementation predates Sprint 10-13 numbering (tag `v0.7-proof-download`): upload (`ProofUpload.tsx`, `proofOfPaymentService.ts`) and download (`proofDownloadService.ts`, role-gated to Direct Remit Officer; `ProofDownloadPage.tsx`). Design in PROOF_MANAGEMENT.md. Sprint 14 implemented the full `ASSIGNED -> PROCESSING -> COMPLETED -> READY_FOR_DOWNLOAD -> DOWNLOADED` chain in code, added the Branch Processing -> Proof Download navigation, and removed the circular dependency between the two. Sprint 15 M1 through M1.75 **proved this chain works at runtime for the first time** (Sprint 14's own verification was entirely static) - uploaded a real batch, assigned it, processed both transactions with real proof images, finalized, and confirmed Proof Download showed the correct 2 completed transactions and 2 proof images, surviving realistic browser navigation. Sprint 15 M2 migrated the last 4 Tailwind components (`ProofDownloadPanel`, `BatchDownloadSummary`, `DownloadHistory`, `BatchDownloadActions`) to shared theme tokens, verified live/screenshot-inspected. Sprint 15 M4 fixed `downloadStatus` mislabeling a `COMPLETED` batch. Remaining, out of scope: a placeholder nav link in `layout/Sidebar.tsx` (different module); `proofOfPaymentService.markProofDownloaded` still orphaned (never called); a minor reactivity gap where the Branch Processing -> Proof Download link doesn't appear until the page is revisited after Finalize (data is correct once revisited). **2026-08-09 (DEC-020, Production Readiness Phase 2 Milestone 2): proof-of-payment files are now real Supabase Storage** (private bucket `proof-of-payment`), not `URL.createObjectURL(file)` blob URLs - `proofOfPaymentService.createProofOfPayment` replaced by `uploadProofOfPayment` (real upload) plus signed-URL helpers; every `previewUrl` a hydrated proof carries is generated fresh at read time. `TransactionProcessingPage.tsx`, a second unreachable implementation of proof upload against a different in-memory model, was deleted rather than migrated - see Branch Processing's row and OPERATIONAL_PERSISTENCE.md. Verified live: an uploaded proof image survived to a Direct Remit Officer's Proof Download session and rendered correctly via its signed URL. See TECH_DEBT.md. |
| Reporting | WORKING END TO END; BUILD RESTORED 2026-08-03 AFTER STRAY TYPE REGRESSION | Sprint 16, stabilized out-of-sprint 2026-08-03 | ~85% (estimate) | **2026-08-03:** an uncommitted, out-of-scope edit to `types/report.ts` had reverted part of its contract to a pre-M4.4 shape (dropping `ReportRow`/`batchReference`/`totals`/four `ReportType` members, reintroducing the three dead row types M4.4 already deleted), breaking `tsc` and `npm run build` against the unmodified `reportService.ts`/`ReportsPage.tsx`/`ReportFilters.tsx`. Fixed by reverting `types/report.ts` to its committed shape; no consumer file needed a change. Build is clean again; see CURRENT_SPRINT.md for detail. Originally built under legacy tags `v0.9.1-reporting-foundation` / `v0.9.2-volume-reports` with six Volume definitions and a working engine. **Sprint 16 M1 found the module had no data source at all**: `ReportsPage.tsx` read from React Router `location.state`, nothing navigated there with state, so every report rendered permanently empty for a real user. Sprint 16 then designed (REPORTING_ARCHITECTURE.md, REPORTING_PROJECTION_LAYER.md) and built the fix across four milestones. **M4.1**: read-only enumerators on `sharedBatchStore` and `branchProcessingQueueService` (DEC-007). **M4.2**: `reportingProjectionService.ts` + `types/reportingProjection.ts` - four async read-only operations producing `BatchReportProjection`, `BranchReportProjection`, `ProcessingReportProjection`, `ProofReportProjection`, reusing every existing summary rather than recomputing it, returning frozen output, enforcing Branch Officer scope in the layer. **M4.3**: Report Service - four async generate operations consuming only the projection layer, one consolidated `matchesFilters` shared with the legacy path, and the first two `PERFORMANCE` definitions (`BRANCH_PERFORMANCE`, `PROOF_COMPLETION`). **M4.4**: `ReportsPage.tsx` migrated off `location.state` onto `reportService`, with loading/empty/error handling built from existing components (none modified) and all legacy dead code removed. **Verified live in a browser across Upload -> Assignment -> Processing -> Proof -> Reports**: all 8 definitions return correct real data, filters work, the empty state works, reports track lifecycle transitions live, and no earlier workflow regressed; a duplicate-React-key defect invisible to `tsc`/`build` was found and fixed. **The M1 headline defect is closed for Reporting.** **2026-08-05:** Decision D-6 resolved - `ProcessingReportProjection`/`BranchReportProjection` now carry real duration data (`processingMinutes`/`averageProcessingMinutes`), computed by the projection layer per its own Section 4.6 design. No new report *definitions* consume it yet (`PROCESSING_TIME`, `OFFICER_PERFORMANCE`'s speed component) - that is separate, deliberately deferred scope. Still open: exports are disabled placeholders (D-3), no Audit category (D-5), branch identity derives from assignments only (D-7), category taxonomy unresolved (D-1). Downstream consumer of Proof Management (PROOF_MANAGEMENT.md, Integration Points). See TECH_DEBT.md. |
| Dashboards | OPERATIONS DASHBOARD WORKING, VERIFIED LIVE; EXECUTIVE/BRANCH NOT STARTED | Sprint 16 | ~60% (estimate) | Not previously listed as a module. Substantial implementation predates the current sprint numbering: `dashboardService.ts`, `types/dashboard.ts`, `OperationsDashboardPage.tsx` (route `/reos/dashboard`, mounted), plus `BranchPerformanceTable`, `WorkQueueTable`, `ExceptionCenter`, `TodaySummary`, `CriticalAlertCard`. It carried the same M1 defect as Reporting - data from `location.state`, every KPI `0` for a real user. **Sprint 16 M4.5 replaced the data source**: `buildOperationsDashboard` now assembles from reporting projections rather than operational entities (output type unchanged, so no component was modified), `reportService.generateOperationsDashboard` supplies them, and the page consumes `reportService` alone. Branch counts come verbatim from `getBranchProcessingQueueSummary` via the projection layer - nothing is recomputed. **Verified live**: every KPI with a data source shows real data, branch/processing/proof statistics are correct, the empty dashboard still works, and the dashboard tracks `READY_FOR_DOWNLOAD -> DOWNLOADED` transitions without a reload. The unreachable `GENERAL_MANAGER` UI was removed as dead code (the type still exists in `types/dashboard.ts`; D-8 open). **2026-08-05:** Transactions Today now reads real data (Sprint 17 M4 captures `transactionDate` on import) and Average Processing Time now reads a real figure instead of permanent "No data" (Decision D-6 - Branch Processing now records completion timestamps). Branch health (`getBranchHealth`) is no longer forced `YELLOW` for every branch by a null speed value. Still open: USD Value / Revenue / USD Processed remain in the view model at `$0.00` because removing the columns requires component changes (**D-9**, now the most visible open item); the Executive and Branch dashboards designed in REPORTING_ARCHITECTURE.md Section 10 are not built (**D-2**). See TECH_DEBT.md. |
| Liquidity Management | COMPLETE, VERIFIED LIVE | Out-of-sprint, 2026-08-08 | ~90% (estimate) | New module. Canonical spec: LIQUIDITY_MANAGEMENT.md. Branch payout accounts (`liquidityStore.ts`, `liquidityService.ts`), manual funding with full history (no approval, no pending state), and a minimal branch registry (`branchRegistryService.ts`, partially closes REPORTING_ARCHITECTURE.md D-7) reusing REOS's own pre-existing, previously-unused `types/branch.ts`. **Existing-architecture review performed first**: a complete, separate Branch Liquidity/Treasury/Funding system already exists outside REOS (`src/pages/branches`, `treasury`, `funding`) - deliberately not reused (different grain: aggregate vs. per-account; different process: approval-gated vs. manual; different data: mocked vs. live). See LIQUIDITY_MANAGEMENT.md Section 3. **Live integration with Branch Processing**: `branchProcessingQueueService.ts` extended with `payoutAccountId`, `startBranchProcessingQueueItem` (the only path into `IN_PROGRESS`, requires an account with sufficient available balance), and `getReservedAmountForAccount` (live-computed, never stored); `completeBranchProcessingQueueItem` now performs the one real deduction, at the one status transition with no reversal path, so no reversal logic exists anywhere. **Reporting integration**: two new projection grains (`LiquidityAccountProjection`, `LiquidityBranchProjection`) plus `FundingEventProjection`, a new `LIQUIDITY` report category, six new report definitions (Branch Liquidity, Daily Consumption, Account Balances, Low Balance Accounts, Liquidity Exceptions, Funding History), and a dedicated Liquidity Dashboard (`liquidityDashboardService.ts`, `LiquidityDashboardPage.tsx`) - the same "one projection layer, separate report/dashboard consumers" pattern Reporting already established. **Verified live in a browser**: add account -> edit -> record funding -> upload/assign a real file -> Branch Processing offers only sufficiently-funded accounts -> start -> complete (balance deducts by the real amount) -> Liquidity Management and Dashboard both show the new balance -> a Branch Liquidity report generates with real data -> Operations Dashboard unaffected (regression check). 16/16 assertions, zero console errors. **A real defect was found and fixed during this verification**: `PayoutAccountManager` and `FundingRecorder`, sibling components reading the same store, did not re-render each other on mutation - fixed by lifting a shared refresh signal to their parent page. **2026-08-08 (DEC-018):** the route is now real Operations-Manager-only RBAC. **2026-08-13 (DEC-021, Production Readiness Phase 2 Milestone 3): this module's data is now Supabase-backed** (`payout_accounts`, `funding_events`, `funding_entries`) - the underlying service functions now have real RLS behind them, closing the role-check gap noted above (matching Branch Processing's equivalent DEC-020 resolution). A real RLS defect was found and fixed during verification: `savePayoutAccount`'s upsert required the INSERT policy even for Branch-Officer-only balance updates, breaking `deductForTransaction` until widened. **Phase 2 (Operational Persistence) is now fully complete** - this was the last in-memory operational store. Verified live: a brand-new Operations Manager session showed a balance correctly deducted by a brand-new Branch Officer session's transaction. **2026-08-16 (DEC-023): the "reconciliation with the legacy Treasury system" item above is now resolved - not by reconciling, but by retiring it.** The physical operational network was confirmed at 8 real branches (`branchRegistryService.ts` seeds 4 of them - a known, separate onboarding gap); the legacy `src/pages/branches`/`treasury`/`funding` tree seeded 12 fictional ones. `BranchListPage.tsx`, `BranchDetailsPage.tsx`, `TreasuryPage.tsx`, `treasuryService.ts`, `types/Treasury.ts`, and `FundingRequestPage.tsx` and their routes/nav links are deleted. Deliberately **not** deleted: `branchService.ts`/`types/Branch.ts`/`fundingRequestService.ts`/`types/FundingRequest.ts` - a dependency check found `src/pages/controller/Dashboard.tsx` and `components/dashboard/BranchStatusTable.tsx` (a separate, unreviewed legacy surface DEC-023 never named) still read from them for KPI aggregates; deleting them would have broken a page outside this decision's scope. **2026-08-16 (DEC-024): `savePayoutAccount`'s upsert-based write is split into `insertPayoutAccount`/`updatePayoutAccountRecord`, and `payout_accounts_insert` RLS is tightened to `OPERATIONS_MANAGER` only** - closing the gap the italicized defect above only provisionally worked around. `payout_accounts_update`/`_select` are unchanged - UPDATE already matched the target grain. Verified via `pg_policies` after applying; **not yet verified with a live negative-path test** (a real Branch Officer session attempting a direct insert, confirmed denied) - every other RLS change in DECISIONS.md has that verification, this one does not yet. See DECISIONS.md DEC-023/DEC-024, TECH_DEBT.md, and OPERATIONAL_PERSISTENCE.md. |
| Import Intelligence | COMPLETE, VERIFIED LIVE (PERSISTENCE + OPERATIONAL DATASET) | Out-of-sprint, 2026-08-08 | ~90% (estimate) | New module - REOS's first genuinely persistent data. Canonical spec: IMPORT_INTELLIGENCE.md. Two new Supabase tables (`import_batches`, `import_beneficiaries`, real `remit-connect` project, RLS enabled with permissive `anon`/`authenticated` policies matching the app's actual unauthenticated posture), approved as **DEC-016** after two genuine blockers were raised and resolved by explicit user decision (persistence approved now; multi-source ingestion scoped to Direct Remit only). `importIntelligenceService.ts` provides SHA-256 file fingerprinting (`computeFileChecksum`), duplicate detection against the ledger by checksum or by batch-reference-plus-reporting-period (`checkForDuplicateImport`), Reporting Period derivation as the mode of real business dates (`deriveReportingPeriod`, never entered manually), and persistence with Replace/Merge/Cancel resolution (`persistImport`). New `ImportIntelligencePage.tsx` (route `/reos/import-intelligence`) renders a Data Coverage grid (source x reporting period, gap-filled, all four unimplemented sources shown honestly as permanently uncovered) and full Import History, both read live from Supabase. **Critical boundary, by design**: wired into `SharedBatchUploadPage.tsx`'s existing Confirm Upload step as purely additive and non-blocking - the live operational workflow (`sharedBatchStore.ts` and everything downstream: Assignment, Branch Processing, Proof Management, Liquidity Management) is completely unchanged, and a Supabase failure only surfaces a dismissible warning, never blocks the upload. **Verified live across three separate browser contexts** (not page reloads within one context) - the second context, performing zero uploads itself, displayed the first context's persisted data, the definitive proof of real cross-session persistence. `tsc`/`build`/`eslint` all clean (`npx eslint` clean except the two already-documented, untouched `Sidebar.tsx` findings). **Extended 2026-08-08 with the Operational Dataset layer (DEC-017, IMPORT_INTELLIGENCE.md Section 13)**: `operationalDatasetService.ts` reads exclusively through `importIntelligenceService.getImportHistory()` - one Supabase query path, transformed in memory - to power a redesigned Data Coverage grid (Year x Month x Source, four statuses: Missing/Imported/Incomplete/Duplicate), a redesigned Import History (Coverage Impact, Validation Outcome, primary/secondary filters, per-batch beneficiary drill-down at `ImportBatchDetailPage.tsx`), a new Duplicate Management page (explains why imports were flagged; deliberately read-only, since Replace/Merge only make sense against a specific incoming file at upload time), and a new Historical Performance page (MoM/YoY growth, source comparison). `ImportIntelligencePage.tsx` is now a lightweight overview linking to all four. A new migration (`import_batches_validation_outcome`) persists the validation summary (valid/invalid/manual-review counts) the operator already saw at Confirm Upload. **Scope decision (DEC-017)**: the prompting instruction described this layer as the read source for Branch Processing and Liquidity Management too; raised as a genuine conflict (neither module's live state was ever in scope, and DEC-016 already deferred that migration) and resolved narrowly - **Branch Processing and Liquidity Management keep their own in-memory state, unaffected.** Verified live: 22/22 assertions, zero console errors, including a real end-to-end Assignment regression check (not a stub). Not built, deliberately: Branch Processing/Liquidity reads through this layer; Branch comparison in Historical Performance (the ledger has no branch dimension); retroactive duplicate resolution; Phase 2 migration of the live operational stores; real authentication. See TECH_DEBT.md. |
| Authentication & Authorization | COMPLETE, VERIFIED LIVE (PRODUCTION READINESS PHASE 1) | Out-of-sprint, 2026-08-08 | ~90% (estimate) | New module - real Supabase Auth replacing the development bypass. Canonical spec: AUTHENTICATION.md. Approved as **DEC-018**, exercising BUSINESS_RULES.md's "Authentication changes" approval gate. `reosAuthService.ts` provides a REOS-scoped session (`login`/`logout`/`getSession`/`onAuthStateChange`/`changePassword`/`getDefaultLandingPath`), deliberately not reusing the legacy `src/services/authService.ts` (different role vocabulary; sources role from user-editable `user_metadata`, a real privilege-escalation risk for RBAC). A new `profiles` table (RLS via a `SECURITY DEFINER` helper avoiding self-recursion) is the real source of role/branch/status; the `admin-create-user` Edge Function is the only path that provisions a real credential, with a self-closing "first user becomes admin" bootstrap rule. `ReosAuthProvider`/`RouteGuards.tsx` (`ReosSessionGate`, `RoleGate`, `BranchGate`, `ReosIndexRedirect`) replace the single identical `localStorage` check every route previously shared with real per-route and per-branch RBAC (Section 6's route-to-role map). Every one of 11 hardcoded actor/role literals found across REOS now carries a real session value; `SharedBatchUploadPage.tsx`'s embedded Assignment panel is now role-aware (actionable for Operations Manager, an informational note otherwise). Import Intelligence's RLS tightened from permissive-`anon` to authenticated-and-role-checked, closing that exact previously-flagged gap. **Verified live in a browser across all three real roles** (Operations Manager, Direct Remit Officer, Branch Officer, each created through the real Create User UI, not seeded): 18/18 assertions, zero console errors. **Three real defects were found and fixed during this verification** - Edge Function CORS (no `Access-Control-Allow-Origin`, invisible to non-browser testing), a stale client-side session after password change causing a silent redirect loop, and relative navigation (`navigate("../" + id)`) breaking once routes became nested under a parent layout route. `tsc`/`build`/`eslint` all clean. Not built, deliberately: failed-login-attempt tracking, an admin password-reset UI, anything depending on outbound email. See TECH_DEBT.md. |
| Administration | PARTIALLY COMPLETE (USER MANAGEMENT REAL) | Out-of-sprint, 2026-08-08 | ~40% (estimate) | User Management (`UserListPage.tsx`, `UserCreatePage.tsx`, `UserEditPage.tsx`, `UserDetailsPage.tsx`, `UserForm.tsx`) is now backed by real Supabase Auth + `profiles` via `userService.ts` (Authentication & Authorization module, DEC-018) - previously an in-memory array, never real. Branch/settings/other master-data administration remains not started. **2026-08-13 (DEC-022, Production Readiness Phase 3): `createUser`/`updateUser` now record `USER_CREATED`/`USER_UPDATED` audit events.** `USER_CREATED` specifically has an observed, unresolved intermittent persistence gap - see the Audit Trail row and TECH_DEBT.md. |
| Audit Trail | COMPLETE, VERIFIED LIVE, ONE KNOWN GAP (PRODUCTION READINESS PHASE 3) | Out-of-sprint, 2026-08-13 | ~90% (estimate) | New module - closes DECISIONS.md D-5, blocked since Sprint 16 M4.2 ("REOS creates audit records but stores none of them," REPORTING_ARCHITECTURE.md Section 5.3). Canonical spec: AUDIT_TRAIL.md. Approved as **DEC-022**. One new append-only table, `audit_events` (no UPDATE/DELETE policy for any role, by design), one unified `AuditEvent`/`AuditAction`/`AuditEntityType` vocabulary (`types/audit.ts`) replacing three previously-separate, previously-discarded shapes (`TransactionProcessingAudit`, `ProofDownloadHistoryEntry`, `SharedBatchReassignmentAudit`). `auditService.recordAuditEvent` is called directly from the service function performing each action (`branchProcessingQueueService.ts`, `branchAssignmentService.ts`, `proofDownloadService.ts`, `liquidityService.ts`, `userService.ts`), never from a page. Actor role is resolved server-side via the existing `current_user_role()` RPC, not accepted as client input. RLS: SELECT is Operations-Manager-only (matching REPORTING_ARCHITECTURE.md Section 8.1's own stated default); INSERT is open to any authenticated role but constrained to the caller's own `auth.uid()`. **A real defect was found and fixed during implementation**: `BATCH_CONFIRMED`'s audit call initially shared a guard with an unrelated Import Intelligence timing precondition, silently skipping both records when an operator confirmed quickly - fixed with its own narrower guard, which also surfaced (without fixing, being out of scope) a pre-existing Import Intelligence gap of the same shape. **Verified live**: one continuous flow (payout account creation/funding, user creation, batch upload/confirm/assign, transaction start/hold/resume/proof-upload/complete, a second transaction start/return, branch finalize, proof download/mark-downloaded) plus an RLS negative check (a Branch Officer's own read attempt against `audit_events` returns zero rows). 17 of 18 audited actions confirmed recorded with correct server-resolved actor role, entity, and branch attribution. **One honestly-recorded, unresolved gap**: `USER_CREATED` events were observed to intermittently not persist despite no reported error; investigated at length (RLS and role-resolution both proven correct via the other 17 action types), a two-attempt retry added as a mitigation but not proven fully effective across repeated test runs - suspected timing interaction specific to auditing an action immediately after an `admin-create-user` Edge Function call, unconfirmed. `BATCH_REASSIGNED` verified by code review only (identical pattern to the verified `BATCH_ASSIGNED`), not driven live. Not built, deliberately: the Audit report category itself (Assignment History, Lifecycle History, Processing History, Proof Download History, User Activity) - this phase persists the trail; report definitions are separate, downstream Reporting work. See TECH_DEBT.md and AUDIT_TRAIL.md. |
| Production Readiness | COMPLETE | Out-of-sprint | 100% | Phases 1-5 complete per DEC-018 through DEC-022. |

## RESOLVED - REOS page mounting (Sprint 14)

A critical defect found during Sprint 14 Milestone 2B: the application's real router `src/routes/AppRoutes.tsx` never mounted `BranchProcessingPage`, `SharedBatchUploadPage`, `BranchAssignmentPage`, `TransactionProcessingPage`, or `ReportsPage`, while `src/features/reos/routes/ReosRoutes.tsx` defined them as dead code nothing imported. Those pages therefore did not ship, despite compiling and building cleanly.

Resolved by the Sprint 14 Route Integration Audit (five routes mounted in the live router) and Stabilization & Closure (`ReosRoutes.tsx` deleted). Verified against the production bundle at the time: previously-absent page strings were present.

Sprint 15 M1 went further and verified this by actually using the app in a browser (upload, assign, navigate) rather than inspecting the bundle - confirming the fix holds for a real user journey, not just that the code is present. Sprint 15 also found (M1) and fixed (M1.5, M1.75) two further defects that bundle-content and `tsc`/`build` checks alone could not have caught: a missing navigation link, and a data-corrupting hydration bug.

Lesson recorded in PROJECT_STATE.md as a standing caveat - a passing build does not prove changed code ships, is reachable, or preserves data correctly.

## Open Item

Proof Management and Reporting have real, substantial implementations in the repository (predating the Sprint 10 Enterprise UI reset) that are not fully represented in ROADMAP.md's sprint-numbering sequence. Proof Management is now formally Sprint 14; Reporting is now formally Sprint 16 (architecture), which partially closes this gap. The Dashboards implementation remains unscheduled and is newly recorded above as its own module row - Sprint 16 found it in the same state as Reporting (implemented, unconnected), and it additionally carries metrics and a role that are out of scope. Recommend a business decision on whether Dashboards is a distinct module or part of Reporting.
